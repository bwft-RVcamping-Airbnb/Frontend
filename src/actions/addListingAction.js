import {ADDING_LISTING, ADDING_LISTING_SUCCESS ,ADDING_LISTING_FAILURE} from '../actions/index';
import { push } from 'connected-react-router';


export const addListingAction = () => {
    return dispatch => {
        
    }
}