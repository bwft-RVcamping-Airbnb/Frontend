const intialState = {
    isLoggedIn: false, 
    user: {},
    errors: ''
}

export const loginReducer = (state=intialState, action) => {

        switch(action.type){
            default: 
                return state;
        }
}